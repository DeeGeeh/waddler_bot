# Copy to .env and set your key. Required for voice/vision (OpenAI) in later phases.
OPENAI_API_KEY=your_key_here

# Motor: rust = Pi GPIO, sim = TCP to Isaac Lab bridge. For sim set MOTOR_BACKEND=sim and SIM_HOST to the machine running the bridge.
MOTOR_BACKEND=rust
SIM_HOST=
SIM_PORT=9999
