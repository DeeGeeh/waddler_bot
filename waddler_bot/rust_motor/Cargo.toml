[package]
name = "rust_motor"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.18", features = ["extension-module"] }

# Raspberry Pi GPIO/PWM; only needed on Linux (Pi)
[target.'cfg(target_os = "linux")'.dependencies]
rppal = "0.17"
